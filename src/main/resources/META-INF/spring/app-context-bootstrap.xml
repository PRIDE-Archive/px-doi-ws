<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:util="http://www.springframework.org/schema/util"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-2.0.xsd
       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.1.xsd">

    <bean id="doiRegister" class="uk.ac.ebi.pride.doi.RegisterDOI">
        <constructor-arg name="metaDataDOI">
            <bean class="uk.ac.ebi.pride.doi.MetaDataDOI">
                <property name="dataOwner" value="${archive.doi.data.owner}"/>
                <property name="email" value="${archive.doi.mail}"/>
                <property name="organization" value="${archive.doi.organization}"/>
                <property name="user" value="${archive.doi.user}"/>
                <property name="password" value="${archive.doi.password}"/>
                <property name="title" value="${archive.doi.title}"/>
            </bean>
        </constructor-arg>
    </bean>

    <util:properties id="doiProperties" location="classpath:META-INF/props/doi.properties"/>

    <context:property-placeholder location="classpath:META-INF/props/*.properties" ignore-unresolvable="true" order="1"/>

</beans>